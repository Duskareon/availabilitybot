# Bot token from https://discord.com/developers/applications
DISCORD_TOKEN=
# Optional: a server ID to sync slash commands to instantly (for testing)
GUILD_ID=
# Optional: where to store the SQLite database
DATABASE_PATH=availability.db
